export function DataFormatsSection() {
  return (
    <>
      <h1 className="govuk-heading-l">Data formats</h1>
      <p className="govuk-body">
        BODS publishes primary data in TransXChange (timetables), SIRI-VM (vehicle location) and
        UK NeTEx (fares). Converted and derived formats may also be available depending on data
        type.
      </p>
    </>
  );
}

